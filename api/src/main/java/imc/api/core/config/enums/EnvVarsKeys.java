package imc.api.core.config.enums;

/**
 * This enum is used to store the keys of the environment variables
 */
public enum EnvVarsKeys {
  DB_URL,
  DB_USERNAME,
  DB_PASSWORD,
  DB_DRIVER_CLASS_NAME,
}
